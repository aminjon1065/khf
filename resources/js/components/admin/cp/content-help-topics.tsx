import { CpContentHelp } from '@/components/admin/cp/content-help';

export function PostEditorHelp() {
    return (
        <CpContentHelp title="Новости и материалы">
            <p>
                Новость публикуется в разделе <code>/язык/news/адрес</code>. Рубрика — одна,
                теги — несколько; оба поля необязательны.
            </p>
            <p>
                Даты «публикации» и «снятия» работают вместе со статусом: запланированный
                материал появится на сайте автоматически. Рабочая копия и кнопка «Обновить
                опубликованную версию» — для правок уже вышедшего текста без снятия с публикации.
            </p>
            <p>
                <kbd className="rounded border px-1 font-mono text-xs">⌘/Ctrl+S</kbd> — сохранить,{' '}
                <kbd className="rounded border px-1 font-mono text-xs">⌘/Ctrl+P</kbd> — превью.
            </p>
        </CpContentHelp>
    );
}

export function ContentHubHelp() {
    return (
        <CpContentHelp title="Коллекции контента">
            <p>
                Каждая карточка — отдельный тип материалов (страницы, новости, документы и т.д.).
                Внутри — единый браузер с поиском, сортировкой, экспортом и импортом JSON/CSV.
            </p>
            <p>
                Используйте <kbd className="rounded border px-1 font-mono text-xs">⌘/Ctrl+K</kbd>{' '}
                в шапке панели: поиск по разделам и по заголовкам записей.
            </p>
        </CpContentHelp>
    );
}

export function GlobalsHelp() {
    return (
        <CpContentHelp title="Глобальные настройки">
            <p>
                Globals — общие данные сайта (логотип, контакты, блок президента, SEO по умолчанию).
                Изменения применяются на всех публичных страницах сразу после сохранения.
            </p>
            <p>
                Для многоязычных полей заполните хотя бы язык по умолчанию; пустые локали не
                выводятся на сайте.
            </p>
        </CpContentHelp>
    );
}

export function MediaLibraryHelp() {
    return (
        <CpContentHelp title="Медиабиблиотека">
            <p>
                Файлы можно складывать в папки, задавать alt-текст по языкам, теги и точку фокуса
                для обложек. В формах контента выбирайте «из библиотеки» вместо повторной загрузки.
            </p>
            <p>
                Вкладка «Использование» показывает, где файл уже привязан. Массовое удаление и
                перемещение — через чекбоксы в сетке.
            </p>
        </CpContentHelp>
    );
}
